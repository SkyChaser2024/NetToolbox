import { featureIcons } from './diagnosticVisuals'

export const toolIcons = {
  home: featureIcons.overview,
  nat: featureIcons.nat,
  ipv6: featureIcons.ipv6,
  ping: featureIcons.ping,
  trace: featureIcons.trace
} as const
